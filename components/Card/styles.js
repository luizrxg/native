import {StyleSheet} from "react-native"

const styles = StyleSheet.create({
    caption: {
        fontSize: 20,
        fontWeight: '600',
        paddingHorizontal: 6
    },
    options: {
        flexDirection: 'row',
    }
});

export default styles